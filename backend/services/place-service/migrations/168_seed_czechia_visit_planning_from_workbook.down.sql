-- Intentionally left irreversible: this country-specific visit-planning overlay
-- replaces temporary placeholders with localized Czechia details. Reverting could
-- remove newer user-facing planning data, so rollback is a no-op.

SELECT 1;
